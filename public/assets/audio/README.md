Drop recorded sounds here as `<id>.ogg` or `<id>.mp3` (for example `chop.ogg`,
`step.ogg`, `thunder.ogg`). The audio manager uses a file when it exists and
falls back to the synthesized placeholder otherwise. Sound ids are listed in
`src/audio/audio.ts`.
